<div id="vue-app">
		<tictac></tictac>
</div>


<script type="x-template" id="tictac">
    <div id="grid">
			<div class="grid-item" v-for="(s, i) in grid" :key="i" @click="gridItemClicked(i)">
				 {{ s }} 
			</div>
    </div>
</script>

<script>

	Vue.component('tictac', 
		{
			template: '#tictac',
			mounted(){
				for (let i = 0; i < 9; i++) {
					this.grid.push(' ');
				}
			},
			data(){
				return {
					grid: [],
					currentChar: 'X'
				}
			},
			methods: {
				gridItemClicked(index){
					if(this.grid[index] == 'X' || this.grid[index] == 'O') return; 
					
					this.$set(this.grid, index, this.currentChar);
					this.currentChar = this.currentChar == 'X' ? 'O' : 'X';
					updateGameState();
				},
				updateGameState(){

				}
				
			},
		}

	)
	var app = new Vue({
			el: '#vue-app',
			mounted() {
			},
	});
		
</script>